import LoginCard from "../organisms/LoginCard";

const LoginForm = () => {
  return (
    <div className="min-h-screen w-full bg-[url('/public/images/BgLogin.jpg')] bg-cover bg-center bg-no-repeat flex items-center justify-center">
      <LoginCard />
    </div>
  );
};

export default LoginForm;
